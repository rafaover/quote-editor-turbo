<main class="container">
  <%= link_to sanitize("&larr; Back to quotes"), quotes_path %>

  <div class="header">
    <h1>New quote</h1>
  </div>

  <%# Instead of Quote.new, we could use @quote or "new_quote" as the frame's target. %>
  <%= turbo_frame_tag Quote.new do %>
    <%= render "form", quote: @quote %>
  <% end %>
</main>
